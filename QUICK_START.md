# 🚀 Швидкий запуск Кринжик бота

## ❌ Якщо бачите помилку "Не знайдено обов'язкові змінні середовища":

### 🏠 Локальний запуск

#### 1. Створіть файл `.env` в папці з ботом:
```env
API_ID=your_api_id_here
API_HASH=your_api_hash_here
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=@your_channel_here
ADMIN_IDS=123456789,987654321
ADMIN_USERNAMES=admin1,admin2
```

### 2. Отримайте необхідні дані:

#### 🤖 Bot Token:
1. Перейдіть до [@BotFather](https://t.me/botfather)
2. Напишіть `/newbot`
3. Дайте ім'я боту
4. Скопіюйте токен

#### 🔑 API ID та Hash:
1. Перейдіть на [my.telegram.org](https://my.telegram.org)
2. Увійдіть з номером телефону
3. Створіть додаток
4. Скопіюйте API ID та API Hash

### 3. Встановіть залежності:
```bash
pip install -r requirements.txt
```

### 4. Запустіть бота:
```bash
python Bot.py
```

## 📋 Приклад правильного .env файлу:
```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
CHANNEL_ID=@mychannel
ADMIN_IDS=123456789
ADMIN_USERNAMES=myadmin
```

---

## 🐳 Docker запуск

### Dockerfile вже готовий! Просто встановіть змінні:

```bash
# Збірка образу
docker build -t krinzhik-bot .

# Запуск з змінними середовища
docker run -d \
  -e API_ID=your_api_id \
  -e API_HASH=your_api_hash \
  -e BOT_TOKEN=your_bot_token \
  -e CHANNEL_ID=@your_channel \
  -e ADMIN_IDS=123456789 \
  -e ADMIN_USERNAMES=yourusername \
  --name krinzhik-bot \
  krinzhik-bot
```

---

## ☁️ Хмарний хостинг (Heroku, Railway, etc.)

### В панелі управління додайте Config Variables:
- `API_ID` = ваш api id
- `API_HASH` = ваш api hash  
- `BOT_TOKEN` = ваш bot token
- `CHANNEL_ID` = @ваш_канал
- `ADMIN_IDS` = 123456789,987654321
- `ADMIN_USERNAMES` = admin1,admin2

### Railway:
1. Підключіть GitHub репозиторій
2. Додайте змінні в Variables
3. Deploy автоматично

### Heroku:
1. Створіть новий додаток
2. Підключіть GitHub
3. Додайте Config Vars
4. Deploy

---

## ⚠️ ВАЖЛИВО:
- Файл `.env` НЕ повинен потрапляти в Git (вже в .gitignore)
- Всі значення мають бути без лапок
- API_ID - це число
- Не діліться токенами з іншими!
- Для публічного репозиторію використовуйте змінні середовища хостингу
