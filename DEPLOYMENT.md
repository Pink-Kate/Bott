# 🚀 Розгортання Кринжик Бота

## 📁 Файли для завантаження в GitHub

### ✅ ЗАВАНТАЖУВАТИ:
- `Bot.py` - основний код бота
- `requirements.txt` - Python залежності
- `README.md` - документація проекту
- `QUICK_START.md` - швидкий старт
- `env.example` - приклад налаштувань
- `.gitignore` - захист приватних файлів
- `Dockerfile` - для Docker
- `docker-compose.yml` - для локального Docker
- `railway.json` - конфігурація Railway
- `DEPLOYMENT.md` - цей файл

### ❌ НЕ ЗАВАНТАЖУВАТИ:
- `B.env` - ваші приватні токени
- `.env` - локальні налаштування
- `*.session` - файли сесії Telegram
- `karma_data.json` - дані користувачів
- `character_data.json` - дані користувачів

## 🔧 Налаштування для різних платформ

### Railway
1. Підключіть GitHub репозиторій
2. Додайте змінні в Variables:
   ```
   API_ID=ваш_api_id
   API_HASH=ваш_api_hash
   BOT_TOKEN=ваш_bot_token
   CHANNEL_ID=@ваш_канал
   ADMIN_IDS=ваш_id
   ADMIN_USERNAMES=ваш_username
   ```

### Heroku
1. Створіть додаток
2. Підключіть GitHub
3. Додайте Config Vars (ті ж змінні що й для Railway)

### Локально
1. Скопіюйте `env.example` в `.env`
2. Заповніть `.env` своїми даними
3. Запустіть: `python Bot.py`

### Docker
1. Скопіюйте `env.example` в `.env`
2. Заповніть `.env`
3. Запустіть: `docker-compose up`
